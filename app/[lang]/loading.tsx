import React from 'react';

interface loadingProps {
    
}

const loading: React.FC<loadingProps> = () => {
    return (
        <div>
            <h1>I ma madafaka loading....</h1>
        </div>
    );
};

export default loading;